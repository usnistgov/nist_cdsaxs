## Installing the package

Use pip from the project root:

```bash
pip install --upgrade pip setuptools wheel
pip install .
```

For contributors, install in editable mode with dev extras:

```bash
pip install -e .[dev]
```

If you prefer conda/mamba, create and activate an environment first, then run the pip commands above.
