I created two modules, one for the gui and one for all the underlying functionality. The idea would be to keep the functionality separate so that it is straighforward to use these in jupyter notebook or other scripts. The gui module would only have the code that builds the interface and its interactive components.

I think this should be a bigger discussion with the team so I'm just sticking some placeholder files in here for now. 
